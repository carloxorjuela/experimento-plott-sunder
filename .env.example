# Archivo de ejemplo de variables de entorno
# Copia este archivo como .env y modifica los valores según sea necesario

# Credenciales de administrador
OTREE_ADMIN_PASSWORD=admin123

# Nivel de autenticación (STUDY para desarrollo, DEMO para público)
OTREE_AUTH_LEVEL=STUDY

# Modo de depuración (True para desarrollo, False para producción)
OTREE_DEBUG=True

# Puerto del servidor (opcional, por defecto 8000)
# OTREE_PORT=8000

# Producción - Base de datos PostgreSQL (opcional)
# DATABASE_URL=postgres://usuario:password@localhost/plott_sunder

# Redis URL (opcional, por defecto redis://localhost:6379)
# REDIS_URL=redis://localhost:6379
